{
  "name": "jsk1-sports-sync-poc",
  "version": "0.1.0",
  "private": true,
  "description": "Phase 1 read-only proof of concept: reads the publicly rendered sports table from the reference site and prints normalised JSON. Does not write anywhere.",
  "license": "UNLICENSED",
  "type": "commonjs",
  "engines": { "node": ">=18" },
  "scripts": {
    "scrape": "node scrape.js",
    "scrape:fixture": "node scrape.js --url file://./fixtures/sample-table.html"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
