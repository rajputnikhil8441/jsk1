{
  "name": "jsk1-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Regression suites for the jsk-1.com site. Run from the repository, not a scratch directory.",
  "scripts": {
    "test": "node run-all.js"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
